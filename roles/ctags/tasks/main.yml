---
- name: Install ctags
  become: true
  apt:
    pkg: ["exuberant-ctags"]
    state: latest
    update_cache: true

- name: Push ctags config
  copy:
    src: ctags
    dest: "{{ ansible_user_registered.home }}/.ctags"
    mode: 0664
